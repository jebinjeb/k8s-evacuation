# setup.py example
from setuptools import setup, find_packages

setup(
    name="k8s-evacuator",
    packages=find_packages(),
    entry_points={
        "console_scripts": [
            "evacuator=evacuator.cli:main",  # cli.py main() function
        ],
    },
)
